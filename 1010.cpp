#include<bits/stdc++.h>
using namespace std;

int main(){

    double a, b, c, d, e, f, total = 0;
    cin >> a >> b >> c >> d >> e >> f;

    total = (b *c ) + (e * f);
    cout << fixed << setprecision(2) << "VALOR A PAGAR: R$ " << total << endl;


    return 0;
}
